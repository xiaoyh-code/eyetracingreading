# 原型驗證記錄

2026-09-24，本機 Python 3.12 + Chromium。

## 已通過

- Python：37 項測試，涵蓋文字切分、PDF／Markdown／DOCX、損壞／空白／超限文件、本機翻譯、同源請求、API 錯誤、生成任務及防止重複提交。
- JavaScript：12 項追蹤測試，涵蓋停留、快速掠過、重讀、抖動、訊號過期、暫停、視窗失焦及文件重設。
- Ruff 及三個瀏覽器模組的 JavaScript 語法檢查通過。
- 真實瀏覽器：停留觸發 serendipity 解釋；放大前後字詞位置及尺寸不變。
- 真實瀏覽器：貼上新文章，以已安裝的本機模型翻譯「The sunlight warms the garden.」得到「陽光溫暖花園」。
- 真實瀏覽器：Markdown、PDF、DOCX 上載後均有可追蹤單字；匯入的 HTML 未被執行。
- 真實瀏覽器：收藏、JSON 匯出、暫停追蹤、390 px 手機版沒有水平溢出。
- 騰訊生圖前端：模擬回應驗證生圖可獨立於雲端文字開關；只在點擊時傳送該字句，並顯示騰訊來源及連結有效期。
- 鏡頭流程：獨立瀏覽器以模擬 WebGazer 完成 45 次校準；5 點驗證沒有額外訓練；已知 13 px 偏移量正確呈現。
- 鏡頭流程：取消、視窗縮放及延遲權限完成後，三個模擬串流均已釋放；測試沒有啟動真實鏡頭。

## Flash Card 更新驗證

- JavaScript 共 24 項測試通過：原有 12 項追蹤測試，加上 12 項卡片排程測試，涵蓋舊收藏、到期邊界、10 分鐘重試、1–90 日進階間隔、去重、損壞資料及不可變更新。
- 獨立 Chromium 工作階段驗證：收藏生字自動成卡、先隱藏答案、空白鍵翻面、數字鍵評分、完成摘要、到期／全部卡片篩選、部分完成後重開及重新整理保留進度。
- 驗證移除／清空收藏亦移除卡片，清空後復原保留原有排程；實際 JSON 下載包含例句及 review 進度。
- 模擬 localStorage 寫入失敗：評分顯示錯誤且不跳到下一張，收藏不產生虛假的成功狀態；恢復儲存後可重試。
- 溫習對話框與生字簿切換、Esc、關閉按鈕的空白鍵，以及保留原本手動暫停狀態均通過。
- 桌面 1280 × 720、手機 390 × 844 及短視窗 390 × 500 驗證；長內容在卡片內捲動，評分按鈕保持可見，沒有水平溢出。翻面後中文解釋立即可見。
- Flash Card 不依賴 API；測試沒有生圖請求或實際鏡頭使用。截圖：output/playwright/flashcard-front.png、flashcard-answer.png、flashcard-mobile.png。

## 實際限制

- 首次 TokenHub 真實生圖測試曾收到餘額／模型額度不足回應；使用者其後回報已可成功出圖，本次視線游標更新沒有再次發出生圖請求。舊式騰訊雲 SecretId／SecretKey、OpenAI 及本機 Stable Diffusion 路徑仍只使用模擬生圖測試。
- 真實 webcam 的單字定位精度取決於使用者設備及校準，測試不能保證實際精度。
- 本機翻譯是小型機器翻譯模型；不是完整的上下文詞典，譯文可能需要人工判斷。
- 掃描 PDF 需要另外 OCR；PDF 複雜版面可能需要校正文稿。
- Python 測試通過時有一項 Starlette 對 httpx 測試客戶端的棄用提示，不影響本次結果。

測試指令及啟動方法見 README.md。瀏覽器測試產物位於 output/playwright/（不加入版本控制）。

## TokenHub 更新驗證

- Python 共 94 項測試通過（含 TokenHub 的官方接口限制、Bearer 鑑權、同步圖片結果、HTTP 錯誤、回應上限、逾時／取消後防止重複提交，以及整合／快取測試）。Ruff 與前端 JavaScript 語法檢查通過。
- 真實 `GET https://tokenhub.tencentmaas.com/v1/models` 回傳 HTTP 200，`hy-image-v3` 狀態為 online；只記錄結果，不記錄密鑰。
- 從獨立瀏覽器按下「用 TokenHub 混元畫張圖」，以示範字 resilience 試一次真實生成；文字 AI 保持關閉。TokenHub 回覆餘額或模型額度不足，本機 API 以安全的中文錯誤回傳 503；沒有取得圖片，亦未自動重試。
- 後續前端驗證攔截圖片 API，以模擬回應確認錯誤提示保留、按鈕恢復、成功圖片顯示及 TokenHub／12 小時期限標示；沒有再次向 TokenHub 提交圖片生成。
- `.env` 被 Git 忽略，權限為僅目前使用者讀寫。`/api/config` 不包含 TokenHub key。

## 視線游標更新驗證

- JavaScript 共 38 項測試通過：原有追蹤及溫習卡測試，加上 14 項游標測試，涵蓋時間平滑、快速轉移、失效／重複／過期／越界樣本、顯示開關、480 ms 隱藏、分頁失焦、捲動、縮放、清理及對話框頂層顯示。修改模組的語法檢查通過。
- 獨立 Chromium 使用模擬 WebGazer 完成校準，通過 24 項流程檢查：游標可到達文章、側欄及頁首；不攔截滑鼠；暫停、失焦、過期和越界會隱藏；關閉顯示仍可追蹤文章單字；生字簿及溫習卡內可顯示，關閉後回到頁面；捲動後保持視窗座標。
- 重新整理後保留顯示選擇，滑鼠模式不顯示視線游標；桌面 1440 × 1000、手機 390 × 844 的游標在視窗邊緣沒有造成水平溢出。
- 取消校準、視窗縮放及延遲權限完成的清理檢查通過，三個模擬串流均釋放。未使用真實鏡頭，也未呼叫圖片生成 API；此測試不代表實際鏡頭精度。截圖：output/playwright/gaze-cursor-page.png。

## 字縫游標及英文發音更新驗證

- 最新游標取代上節全頁自由圓點：只吸附文章字縫／行首尾；文章外及對話框隱藏。JavaScript 共 76 項測試通過，包括 18 項新增字縫幾何／穩定時間／生命週期測試及 20 項英文發音測試；原有閱讀追蹤及卡片排程亦通過。
- Python 共 126 項測試通過，新增 32 項本機發音測試覆蓋單字驗證、聲線探測、WAV 格式、並行請求限制、逾時／取消清理、臨時檔案刪除及 API 同源限制。Ruff 通過。測試停用 dotenv 載入，沒有雲端 TTS 或生圖請求。
- 獨立 Chromium 以模擬視線通過 13 項檢查：45 次校準及獨立驗證、小幅抖動時游標座標完全不變、字縫不遮蓋文字、確認後更新單字目標、文章外隱藏、開關與暫停、對話框，以及字體／捲動後重算。沒有開啟真實鏡頭。
- 另外 12 項瀏覽器發音流程檢查通過：浮動／詞義／卡片喇叭、再次點擊停止、文章版面不移位、喇叭的空白鍵不翻面、關閉卡片停止播放、手機無水平溢出，以及瀏覽器無本機聲音時的 localhost 音訊後備流程。播放事件使用模擬，不測試實際喇叭音量。
- 真實本機 `POST /api/speech` 成功輸出 serendipity WAV：55,426 bytes、22,050 Hz、單聲道 PCM16、1.164 秒，含非零音訊資料；未向系統喇叭播放。macOS 聲線可用，無需 API key。
- 本機服務已重新啟動；介面及非 vendor 靜態檔案加上 no-cache，避免更新時新舊模組混用。截圖：output/playwright/gaze-snap.png、pronunciation-card.png、pronunciation-mobile.png。

## API 設定及學習簿入口更新驗證

- 153 項相關後端回歸測試通過（含 41 項設定測試），Ruff 通過。涵蓋原子儲存、0600 權限、其他設定／註解保留、BOM／換行／引號、磁碟失敗回滾、不回顯憑證，以及保存後重啟的環境變數優先順序。
- 生圖中不可更新設定，即使瀏覽器已取消等待，底層任務未完仍回傳 409；修改設定保留付費快取、速率限制及未確認任務保護。保存本身不呼叫 TokenHub。
- 獨立 Chromium 連接另一個測試伺服器及臨時設定檔，以合成 Key 通過 19 項流程檢查：輸入遮罩、未填密鑰錯誤、提交／Esc 清空、留空保留、回應不含 Key、localStorage 不含 Key、重新整理保持狀態、即時啟用手動生圖，以及慢速啟動回應不可覆蓋剛儲存的設定。
- 學習簿的頁首／側欄入口、收藏後卡片溫習、重新整理保留資料、390 px 手機版及設定對話框無水平溢出均通過。JavaScript 語法檢查通過。
- 測試沒有更改使用者現有 Key、沒有呼叫生圖或使用真實鏡頭。本機正式服務已重新啟動；唯讀確認現有 TokenHub 設定和本機發音仍可用。
- 最後啟動檢查發現 macOS 聲線名稱新增語言標籤；已支援 `Samantha (English (US))` 等已安裝名稱。33 項語音測試及 Ruff 通過，真實本機生成 hello WAV 成功（35,696 bytes），未播放或呼叫雲端。
- 截圖：output/playwright/api-settings.png、api-settings-mobile.png、learning-notebook-mobile.png。
