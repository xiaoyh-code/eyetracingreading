# Reference only: the application does not load .env files.
# Cloud API keys are entered in the webpage and kept only in browser memory.
# Never put cloud keys in this file or export them for the local server.
# Optional: export LOCAL_IMAGE_URL to use an offline Stable Diffusion WebUI.
# Start your own WebUI with --api; only a loopback HTTP URL is accepted.
LOCAL_IMAGE_URL=
